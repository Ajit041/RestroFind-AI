'use client'

import { useEffect } from 'react'
import { supabase } from '@/lib/supabase'
import { useRouter } from 'next/navigation'

export default function CallbackPage() {
  const router = useRouter()

  useEffect(() => {
    const handleAuth = async () => {
      try {
        await supabase.auth.getSession()
        router.push('/') // Redirect to home after auth
      } catch (error) {
        console.error('Auth callback error:', error)
        router.push('/error') // Or show error message
      }
    }

    handleAuth()
  }, [])

  return <div>Loading...</div>
}

app/auth/reset/page.tsx (for password reset)

'use client'

import { useEffect } from 'react'
import { supabase } from '@/lib/supabase'
import { useRouter } from 'next/navigation'

export default function ResetPage() {
  const router = useRouter()

  useEffect(() => {
    router.push('/') // Or show success message
  }, [])

  return <div>Password reset instructions sent to your email.</div>
}